<?php

use Illuminate\Database\Seeder;

class ConversationSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        // you can use model factories to conveniently generate large amounts of database records.
        // factory(App\Conversation::class, 5)->create()->each(function ($user) {
        // $user->conversations()->save(factory(App\Conversation::class)->make());
        // });
    }
}
